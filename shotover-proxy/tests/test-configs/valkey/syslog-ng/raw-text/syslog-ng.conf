@version: 4.8
@include "scl.conf"

source s_local {
    internal();
};

source s_network {
    udp(ip("0.0.0.0"));
    tcp(port(601));
};

destination d_local {
	file("/var/log/syslog");
};

log {
	source(s_local);
	source(s_network);
	destination(d_local);
};